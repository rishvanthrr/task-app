apiVersion: v1
kind: Service
metadata:
  name: task-service
spec:
  type: NodePort
  selector:
    app: task-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007